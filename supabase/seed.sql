-- Optional seed data for local development
-- Run after migration: supabase db reset (runs both migrations + seed)
-- Or manually: psql $DATABASE_URL < supabase/seed.sql

-- Nothing required here — the app creates companies and users at runtime.
-- Add dev fixtures below if useful for testing.

-- Example: mark the local dev admin email so admin panel works
-- (The API reads ADMIN_EMAILS env var; this seed is for reference only)
